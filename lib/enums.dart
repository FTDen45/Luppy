enum MenuState { search, favourite, mainMenu, profile, burger }
enum GenderIcon { men, woman }
enum MessageLogo { message, dinDone, star }
enum Who { my, you }
enum CloudPosition {
  first, //коммент сверху
  center, //коммент по середине
  last, //коммент последний
  one, //коммент один
}
